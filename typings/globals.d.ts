/**
 * Constant variable that determines if the code is currently running on development environment.
 * Any code inside `if (__DEV__) { ... }` block will be eliminited in production.
 */
declare var __DEV__: boolean;
